[package]
name = "jelto-tauri-example"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-jelto = { path = "../.." }

[build-dependencies]
tauri-build = { version = "2", features = [] }
