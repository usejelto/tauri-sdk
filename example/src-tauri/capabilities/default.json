{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Jelto calls from the example's local main window only",
  "local": true,
  "windows": ["main"],
  "permissions": ["core:default", "jelto:default"]
}
